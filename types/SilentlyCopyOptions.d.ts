/**
 * SilentlyCopy options.
 *
 * @interface SilentlyCopyOptions
 */
interface SilentlyCopyOptions {
	/**
	 * Force to overwrite.
	 *
	 * @type {boolean}
	 * @memberof SilentlyCopyOptions
	 */
	overwrite: boolean;
}
